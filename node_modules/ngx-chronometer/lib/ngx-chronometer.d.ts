import { Subject, Subscription } from 'rxjs';
export declare enum TimeChonometer {
    timemedio = 0,
    timeone = 1,
    timetwo = 2
}
export declare enum StatusChonometer {
    desactived = 0,
    pause = 1,
    start = 2,
    finish = 3,
    restart = 4,
    stop = 5,
    refresh = 6
}
export declare class Chronometer {
    id: number;
    second: number;
    limitSecond: number;
    status: StatusChonometer;
    timeLabel: TimeChonometer;
    maxSecond: number;
    maxMinute: number;
    maxHour: number;
    time: Array<number>;
    onChronometer: Subject<Chronometer>;
    intervalSub: Subscription;
    /**
     * @description Constructor of class
     * @param o Object default = {}
     */
    constructor(o?: any);
    start(): void;
    pause(): void;
    restart(): void;
    stop(): void;
    refresh(): void;
}
